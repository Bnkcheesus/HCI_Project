// Implements Gain 2 / Gain Creator 2 — carry the kiosk's shelf-location map on your
// phone via QR handoff, instead of memorizing the route.
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export function QrPage() {
  return <PlaceholderScreen title="Mobile — QR" figmaNodeId="41:598" tracedTo="Gain 2 / Gain Creator 2" />
}
