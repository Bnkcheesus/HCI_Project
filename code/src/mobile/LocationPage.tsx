// Implements Gain 2 / Gain Creator 2 / Product-Service 2 — shelf-location map on the
// phone, mirrors the kiosk's MapView/FloorMap.
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export function LocationPage() {
  return (
    <PlaceholderScreen
      title="Mobile — Định vị kệ sách"
      figmaNodeId="41:630"
      tracedTo="Gain 2 / Gain Creator 2 / Product-Service 2"
    />
  )
}
