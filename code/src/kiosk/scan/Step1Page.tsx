// Implements Job 3 / Pain Reliever 3 / Product-Service 3 — scan step 1 (book code).
// Also covers the failure state (kiosk-book-scan-step1-fail) via component state,
// not a separate route.
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export function ScanStep1Page() {
  return (
    <PlaceholderScreen
      title="Kiosk — Quét sách (bước 1)"
      figmaNodeId="20:366 (fail: 39:82)"
      tracedTo="Job 3 / Pain Reliever 3 / Product-Service 3"
    />
  )
}
